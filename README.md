# Poem-Song Alignment via Contrastive Learning

Cross-modal retrieval system that recommends songs matching the mood/theme of poems using learned embeddings.

## Setup

```bash
conda env create -f [environment.yml](http://_vscodecontentref_/1)
conda activate cs229